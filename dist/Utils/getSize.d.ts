import { ISize } from "../Document/Generics";
export declare function getImageSize(size: string | number | ISize): ISize;
export declare function getSize(size: string | number, pageWidth?: number): number;
